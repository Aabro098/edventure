class MessageModel {
   String? id;
   String? currentMessage;
   bool? select = false;
   String? time;

  MessageModel({
    this.id,
    this.currentMessage,
    this.select,
    this.time,
  });
}
